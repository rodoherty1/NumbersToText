// Define the module.
define(
    [
        /* No dependencies. */
    ],
    function(){
		'use strict';
		
		var NumbersToText;
		
		NumbersToText = {
			translate : function (number) {
				return 'ONE';
			}
        };

        return NumbersToText;
    }
);